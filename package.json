{
  "name": "go-studio-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "plugins/*",
    "go-stack-mcp",
    "templui-mcp"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
